name: Test ARC runners

on:
  workflow_dispatch:

jobs:
  ubuntu-2404:
    runs-on: ubuntu-latest
    steps:
      - name: Check runner
        run: |
          echo "Runner: $RUNNER_NAME"
          echo "OS:"
          cat /etc/os-release
          echo "Kernel:"
          uname -a
          